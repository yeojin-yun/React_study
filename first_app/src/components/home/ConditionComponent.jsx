import CardContainer from "./CardContainer";

const Badge = ({}) => {
  return;
};

function ConditionalComponent() {
  return (
    <CardContainer
      title="4. 조건부 Props"
      subTitle="Props 값에 따라 다르게 동작하는 컴포넌트!"
    >
      <p>⬆️태그들을 클릭해서 제거해보세요</p>
    </CardContainer>
  );
}
export default ConditionalComponent;
